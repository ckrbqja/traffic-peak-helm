apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    name: result-api
  type: NodePort
  ports:
    - port: 1380
      targetPort: 1380
      protocol: TCP
